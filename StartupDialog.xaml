<Window x:Class="OperatorScriptWpf.StartupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Данные для скрипта"
        Height="320"
        Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
        
        <Grid Margin="20">
                <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="24"/>
                        <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0"
                           Text="Перед началом работы заполните данные для подстановки в скрипт"
                           TextWrapping="Wrap"
                           FontSize="15"
                           FontWeight="SemiBold"/>
                
                <StackPanel Grid.Row="2">
                        <TextBlock Text="Введите свое имя"/>
                        <TextBox x:Name="OperatorNameTextBox"
                                 Margin="0,6,0,0"
                                 Height="30"/>
                </StackPanel>
                
                <StackPanel Grid.Row="4">
                        <TextBlock Text="Введите имя и отчество клиента"/>
                        <TextBox x:Name="ClientNameTextBox"
                                 Margin="0,6,0,0"
                                 Height="30"/>
                </StackPanel>
                
                <StackPanel Grid.Row="6"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                        <Button Content="Сохранить"
                                Width="120"
                                Height="34"
                                IsDefault="True"
                                Click="SaveButton_Click"/>
                </StackPanel>
        </Grid>
</Window>